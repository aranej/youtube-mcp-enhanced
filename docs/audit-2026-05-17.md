# YouTube MCP Audit - 2026-05-17

Timestamp: `2026-05-17T17:15:00+02:00`

## Verdict

Pouzitelny zaklad, nie hlinene nohy.

Server realne funguje v Codex App cez Windows launcher, YouTube Data API tooly prechadzaju a transcript fallback funguje na CZ/SK/EN videach. Pred navodom pre dalsieho Windows pouzivatela bolo treba dotiahnut par praktickych veci: produkcny dependency audit, server version, URL polia pre channel/playlist video vystupy a drobne warningy.

## Local Configuration

Codex config:

- `C:\Users\Jozef\.codex\config.toml`
- `[mcp_servers.youtube]`
- command: `powershell.exe`
- launcher: `C:\AIprojekty\Codex\2026-04-19_21-17-youtube-mcp-enhanced\scripts\launch-youtube-mcp.ps1`
- `startup_timeout_sec = 60.0`
- `tool_timeout_sec = 180.0`
- env includes `YOUTUBE_API_KEY`, `YOUTUBE_OAUTH_CLIENT_ID`, `YOUTUBE_OAUTH_CLIENT_SECRET`

Secrets were not copied into this audit.

## Smoke Videos

Test inputs:

- CZ: `https://youtu.be/lL1toTtiI28?si=oCMtLl0UC3INeO_2`
- SK: `https://youtu.be/rBFyq3_-UE0?si=zsPUTiSB3ycd3ymJ`
- EN: `https://youtu.be/hDn8-fK3XaU?si=VbzqgQhIdqAlS0IK`

Metadata via MCP `videos_getVideo`:

- CZ: OK, title `Krásný omyl: co mě iPhone Air naučil o životě (ne o telefonech)`, language `cs`, duration `PT13M18S`
- SK: OK, title `Milan Dubec ma hejtuje, ale nemá dôvod :)`, language `sk`, duration `PT9M`
- EN: OK, title `It's finally here.`, language `en`, duration `PT12M38S`

Transcript smoke after build:

- `lL1toTtiI28` / `cs`: OK, 691 entries, first `iPhone Air používám posledních osm`
- `rBFyq3_-UE0` / `sk`: OK, 508 entries, first `Milan Dubec ma hejtuje. Prvýkrát bude`
- `hDn8-fK3XaU` / `en`: OK, 790 entries, first `Coding's changing faster than ever. I`

Other MCP smoke:

- `videos_searchVideos`: OK, exact CZ video found with top-level `url` and `videoId`
- `videos_getComments`: OK, SK video returned comments and pagination token
- `channels_getChannel`: OK for Theo channel
- `channels_listVideos`: OK
- `playlists_getPlaylistItems`: OK for Theo uploads playlist
- `youtube_checkAuth`: OK, OAuth configured and existing credentials valid

Launcher smoke:

- `scripts\launch-youtube-mcp.ps1` passed MCP `initialize`
- `tools/list` returned expected read/write tool list
- `tools/call videos_getVideo` worked through the launcher
- after fix, server reports version `1.0.0`

## Fixes Applied

- Updated `package-lock.json` with `npm audit fix`; production audit now has 0 vulnerabilities.
- Kept full audit low-severity dev-only issue as residual because it is behind `@smithery/cli` and fixing it likely requires a larger major dev-tool upgrade.
- Aligned MCP server version from hardcoded `0.1.12` to `1.0.0`.
- Removed duplicate CLI success log path.
- Changed `youtube_startAuth` background promise to explicit `void`.
- Removed one unused OAuth catch variable.
- Added top-level `url` and `videoId` to `channels_listVideos` results.
- Added top-level `url` and `videoId` to `playlists_getPlaylistItems` results.

## Validation

Commands:

```powershell
npm run typecheck
npm run lint
npm run build
npm audit --omit=dev
npm audit
py -3 -m yt_dlp --version
```

Results:

- `npm run typecheck`: OK
- `npm run build`: OK
- `npm audit --omit=dev`: OK, 0 vulnerabilities
- `npm run lint`: OK with 6 warnings, all `no-explicit-any`
- `npm audit`: 6 low vulnerabilities remain through dev-only `@smithery/cli`
- `py -3 -m yt_dlp --version`: OK, `2026.03.17`

## Residual Risks

P2:

- Current repo has no test suite. Core behavior is covered only by smoke tests.
- Transcript extraction depends on `yt-dlp` availability through Python launcher. It is robust on this machine, but setup docs for another Windows machine must include this explicitly.
- `package.json` and MCP server version can drift again because server version is still a constant.

P3:

- `npm run lint` still reports six `any` warnings.
- Full `npm audit` still reports low severity dev-only `@smithery/cli` chain.
- Startup stderr includes Node `DEP0040 punycode` warning from dependencies.
- README still needs a Codex App for Windows setup section tailored for another user.

## Do Not Do Yet

- Do not run `npm audit fix --force` just to silence low dev-only advisories.
- Do not test write tools like `playlists_create`, `playlists_addVideo`, or `playlists_addVideos` without explicit OAuth/write approval.
- Do not publish or package for another user until the Windows setup guide is written and tested from a fresh clone.

## Next Recommended Step

Create a Slovak Windows setup guide for another Codex App user:

- prerequisites: Node 20+, Git, Python launcher, `yt-dlp`
- clone/build commands
- Codex `config.toml` block with placeholders
- how to create/use YouTube Data API key
- optional OAuth setup for playlist writes
- restart Codex App
- smoke tests using the three URLs above
- troubleshooting checklist
